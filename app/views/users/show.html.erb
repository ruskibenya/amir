<!-- TODO: FONT AWESOME DOESN'T SEEM TO WORK HERE, only for fa-map weirdly -->

<% content_for :above_container do %>
  <div class="profil-section">
    <div class="info">
      <div class="user-pp">
        <%= image_tag ("#{current_user.profile_picture}"), class: 'avatar-large' %>
      </div>
      <div class="user-name">
        <h4><%= current_user.name %>   <i class="far fa-edit"></i></h4>
      </div>
    </div>
    <div class="button-create">

      <%= link_to destroy_user_session_path, class: 'amir-btn', method: :delete do %>
        <i class=“fa fa-sign-out”></i> <%= t(".sign_out", default: "Log out") %>
      <% end %>
    </div>
  </div>
<% end %>

<% if flash[:invitations_sent] %>
    <div class="notice">Invitations were sent!</div>
<% end %>

<div class="tours-section">
  <div class="create-button-section align-text">
    <%= link_to 'create a new tour', new_tour_path, class: 'simple-link' %>
  </div>

  <h4 class="title-page">tours</h4>

  <div class= "col-xs-12 col-sm-12 col-md-12" >

  </div>
  <% current_user.tours.each do |tour| %>
    <%= render "components/tour_card", tour: tour %>
  <% end %>
</div>
